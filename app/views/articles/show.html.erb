<h2><%= @article.title %></h2>
<p><%= @article.content %></p>

<p><%= link_to "Edit an existing article", edit_article_path() %></p>
<%= link_to "Delete",
  article_path(@article.id),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
